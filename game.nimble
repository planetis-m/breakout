# Package

version       = "1.0"
author        = "planetis-m"
description   = "Breakout game implemented using strict ECS architecture. Used as a testbed."
license       = "MIT"

# Dependencies

requires "nim >= 1.5.0"
requires "bingo >= 0.4.1"
requires "sdl2#head"
requires "fusion#head"
